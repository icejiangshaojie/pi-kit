/**
 * Retained as a no-op for rollback compatibility.
 *
 * Pi's explicit --tools allowlist is the source of truth. Enabling every
 * built-in and extension tool here bypassed read-only subagent boundaries.
 */
export default function () {}
